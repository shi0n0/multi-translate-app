databases:
  - name: ai_translate_db
    region: singapore
    plan: free
    databaseName: ai_translate_db
    user: translate_user

services:
  - type: web
    name: AI-Translate
    env: python
    region: singapore
    buildCommand: './build.sh'
    startCommand: 'gunicorn mysite.wsgi:application'
    plan: free
    branch: main
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: AI-Translate-DB
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
    autoDeploy: true